@manifest_fulfilled_value = 0.75

# FALLBACK MANIFESTS - unused outside fallback templates

fallback_strength_in_unity = {
	name = fallback_strength_in_unity_name
	description = fallback_strength_in_unity_desc
	is_manifest = yes
	ratio_progress = {
		total_amount = 20
		completed_amount_collection = {
			input = game:scope
			operators = { faction_members }
			name = COLLECTION_FACTION_MEMBERS
		}
		
		scale = {			
			stability_factor = 0.15
		}
		
		progress_sections = {
			manifest_fulfilled = {
				min = @manifest_fulfilled_value
	
				modifier = { 
					political_power_factor = 0.1
				}			
			}
		}
	}
}

fallback_regional_expansion = {
	name = fallback_regional_expansion_name
	description = fallback_regional_expansion_desc
	is_manifest = yes
	ratio_progress = {
		total_amount_collection = collection:states_on_faction_leader_continent
		completed_amount_collection = collection:controlled_states_on_faction_leader_continent

		scale = {
			party_popularity_stability_factor = 0.1
		}
		
		progress_sections = {
			manifest_fulfilled = {
				min = @manifest_fulfilled_value
				modifier = { 
					war_stability_factor = 0.1
				}
			}
		}
	}
}

# GENERIC MANIFESTS

### World Revolution (Socialist countries)
generic_manifest_world_revolution = {
	# Spread socialism to as many non-capitulated countries as possible
	name = generic_manifest_world_revolution_name
	description = generic_manifest_world_revolution_desc
	is_manifest = yes
	ratio_progress = {
		total_amount = game:all_countries
		completed_amount_collection = collection:world_socialist_countries

		scale = {
			war_support_factor = 0.15
		}

		progress_sections = {
			manifest_fulfilled = {
				min = @manifest_fulfilled_value
				modifier = { 
					war_stability_factor = 0.1
				}
			}
		}
	}
}

### New World Order (Fascist countries)
generic_manifest_new_order = {
	# Spread socialism to as many non-capitulated countries as possible
	name = generic_manifest_new_order_name
	description = generic_manifest_new_order_desc
	is_manifest = yes
	ratio_progress = {
		total_amount = game:all_countries
		completed_amount_collection = collection:world_fascist_countries

		scale = {
			war_support_factor = 0.15
		}

		progress_sections = {
			manifest_fulfilled = {
				min = @manifest_fulfilled_value
				modifier = { 
					war_stability_factor = 0.1
				}
			}
		}
	}
}

### Defeat Socialism (Non-Socialist countries)
generic_manifest_defeat_socialism = {
	# Spread socialism to as many non-capitulated countries as possible
	name = generic_manifest_defeat_socialism_name
	description = generic_manifest_defeat_socialism_desc
	is_manifest = yes
	ratio_progress = {
		total_amount = game:all_countries
		completed_amount_collection = collection:world_non_socialist_countries

		scale = {
			party_popularity_stability_factor = 0.2
		}

		progress_sections = {
			manifest_fulfilled = {
				min = @manifest_fulfilled_value
				modifier = { 					
					stability_factor = 0.1
				}
			}
		}
	}
}

### Defeat Fascism (Non-Fascist countries)
generic_manifest_defeat_fascism = {
	# Spread socialism to as many non-capitulated countries as possible
	name = generic_manifest_defeat_fascism_name
	description = generic_manifest_defeat_fascism_desc
	is_manifest = yes
	ratio_progress = {
		total_amount = game:all_countries
		completed_amount_collection = collection:world_non_fascist_countries

		scale = {
			party_popularity_stability_factor = 0.2
		}

		progress_sections = {
			manifest_fulfilled = {
				min = @manifest_fulfilled_value
				modifier = { 					
					stability_factor = 0.1
				}
			}
		}
	}
}

### Regional Defense (Non-Fascist countries)
generic_manifest_regional_defense = {
	# Spread socialism to as many non-capitulated countries as possible
	name = generic_manifest_regional_defense_name
	description = generic_manifest_regional_defense_desc
	is_manifest = yes
	ratio_progress = {
		total_amount = collection:states_on_faction_leader_continent
		completed_amount_collection = collection:controlled_states_on_faction_leader_continent

		scale = {
			army_core_defence_factor = 0.1
		}

		progress_sections = {
			manifest_fulfilled = {
				min = @manifest_fulfilled_value
				modifier = { 					
					recruitable_population_factor = 0.1
				}
			}
		}
	}
}

### Global Hegemony (Non-Democratic countries)
generic_manifest_global_hegemony = {
	# Spread socialism to as many non-capitulated countries as possible
	name = generic_manifest_regional_defense_name
	description = generic_manifest_regional_defense_desc
	is_manifest = yes
	ratio_progress = {
		total_amount = game:all_states
		completed_amount_collection = collection:controlled_states_by_faction

		scale = {
			war_support_factor = 0.1
		}

		progress_sections = {
			manifest_fulfilled = {
				min = @manifest_fulfilled_value
				modifier = { 					
					weekly_casualties_war_support = 0.02
				}
			}
		}
	}
}

# HISTORICAL MANIFESTS

### NEW ORDER IN EUROPE
manifest_new_order_in_europe = {
	# Spread fascism to as many states in Europe as possible
	name = manifest_new_order_in_europe_name
	description = manifest_new_order_in_europe_desc
	is_manifest = yes
	ratio_progress = {
		total_amount = collection:states_in_europe
		completed_amount_collection = collection:states_in_europe_controlled_by_fascists

		scale = {
			fascist_drift = 0.05
			weekly_casualties_war_support = 0.01
		}

		progress_sections = {
			manifest_fulfilled = {
				min = @manifest_fulfilled_value
				modifier = {
					war_stability_factor = 0.25
				}
			}
		}
	}
}